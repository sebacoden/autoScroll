package com.freelanzer.autoscroller.ui.navigation

/**
 * Rutas del grafo de navegación. Centralizadas como constantes para evitar typos.
 *
 * [SETTINGS] es la pantalla principal (post-onboarding). [APP_PICKER] es el selector de
 * apps para la allowlist de activación.
 */
object AppRoutes {
    const val EULA = "eula"
    const val SETTINGS = "settings"
    const val APP_PICKER = "app_picker"
}
